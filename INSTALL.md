# 🚀 Installation Guide

## Быстрая установка (Рекомендуется)

### Windows:

1. **Откройте PowerShell или CMD в папке проекта**

2. **Запустите quick-setup.bat:**
   ```cmd
   quick-setup.bat
   ```

3. **Введите путь к вашему RAGE MP серверу:**
   ```
   D:\newservertest
   ```

4. **Готово!** Скрипт автоматически:
   - Установит зависимости
   - Скомпилирует TypeScript
   - Скопирует все файлы на сервер

### Linux/MacOS:

```bash
chmod +x deploy.sh
./deploy.sh /path/to/your/server
```

---

## Ручная установка

### Шаг 1: Установка зависимостей

```bash
npm install
```

### Шаг 2: Компиляция TypeScript

```bash
npm run build
```

### Шаг 3: Развертывание на сервер

#### Windows:
```cmd
deploy.bat "D:\path\to\your\server"
```

#### Linux/Mac:
```bash
./deploy.sh /path/to/your/server
```

---

## Структура после установки

```
D:\newservertest\
├── ragemp-server.exe
├── conf.json
├── packages/
│   └── gamemode/              ← Gamemode package
│       ├── index.js
│       └── package.json
├── dist/                      ← Compiled TypeScript
│   ├── index.js
│   ├── ragemp/
│   ├── database/
│   ├── services/
│   └── types/
├── node_modules/              ← Dependencies
└── database/                  ← Auto-created on first run
    ├── data.json
    └── jobs.json
```

---

## Запуск сервера

```cmd
cd D:\newservertest
ragemp-server.exe
```

### Вы должны увидеть:

```
[INFO] Loading NodeJS packages...
[GameMode] Loading TypeScript gamemode...
[Server] GameMode started!
[Server] Database initialized
[Server] Available jobs: police,taxi,miner
[Server] All events and commands registered!
[GameMode] TypeScript gamemode loaded successfully!
```

---

## Обновление сервера

### После изменения кода:

```cmd
update.bat "D:\path\to\your\server"
```

Это:
1. Пересоберет TypeScript
2. Скопирует обновленные файлы
3. Вы просто перезапустите RAGE MP сервер

---

## Автоматическое обновление при разработке

```cmd
npm run watch:deploy "D:\path\to\your\server"
```

Теперь при каждом изменении `.ts` файла:
- Автоматически компилируется
- Автоматически копируется на сервер
- Просто перезапустите RAGE MP

---

## Проверка установки

1. Запустите сервер
2. Подключитесь к серверу
3. Введите команду: `/help`
4. Вы должны увидеть список команд

### Тестовые команды:

```
/stats           # Ваша статистика
/jobs            # Список работ
/hire taxi       # Устроиться таксистом
/duty            # Начать смену
/deposit 1000    # Положить в банк
```

---

## Troubleshooting

### Проблема: "Cannot find module"

**Решение:**
```bash
npm install
```

### Проблема: TypeScript errors при сборке

**Решение:**
```bash
npm install
npm run build
```

### Проблема: Gamemode не загружается

**Проверьте:**
1. Файл `packages/gamemode/index.js` существует
2. Папка `dist/` существует и содержит файлы
3. Папка `node_modules/` скопирована
4. В `conf.json` параметр `"enable-nodejs": true`

### Проблема: База данных не сохраняется

**Проверьте:**
1. Папка `database/` создана
2. Права на запись в папку
3. Диск не заполнен

---

## Следующие шаги

1. ✅ Сервер запущен
2. 📝 Изучите команды: `/help`
3. 🎨 Настройте CEF интерфейсы (см. README.md)
4. 🔧 Добавьте свои механики (см. RAGEMP_SETUP.md)
5. 🚀 Запустите production сервер

---

## Полезные ссылки

- [README.md](README.md) - Полная документация
- [RAGEMP_SETUP.md](RAGEMP_SETUP.md) - RAGE MP интеграция
- [GitHub Repository](https://github.com/20XMAS24/ragemp-server-starter)

---

**Готово! Ваш сервер работает! 🎉**
